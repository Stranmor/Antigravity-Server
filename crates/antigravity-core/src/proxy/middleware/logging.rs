// logmiddleware
// directlyuse tower_http::trace::TraceLayer::new_for_http() atroutein

#[cfg(test)]
mod tests {
    #[test]
    fn test_logging_middleware() {
        // Logging middleware uses tower_http::trace::TraceLayer directly
    }
}
